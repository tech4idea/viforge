1. 修改会话区域的默认展示宽度，和中间编辑预览区域保持一致
2. 工作区域的项目目录显示，当打开其中项目目录时，会调整文件夹缩进，让人看起来像是上面项目的子目录，这个需要调整一下，即便处于打开状态，视觉上依然要和目录保持平级
3. 全局搜索，替换 ViForge 旧工程名为 ViForge，包括环境变量，注册表 key 等，后续不再保留旧工程名叫法
4. 创作项目区域为空时，给个占位提示，引导用户创建新项目
5. 同时修复后台报错：apps/api dev: Error in handler LangChainTracer, handleLLMEnd: Error: No LLM run to end.
6. 微信连接码居中显示，且扫码连接成功后，自动刷新为已连接状态，目前是要下次打开才能看到已连接
7. 会话运行限制逻辑，改为只限制后端实际运行，用户可提前提交对话内容，但要再后台排队，包括定时任务执行，执行时如果遇到本会话正在运行，也需要排队；排队中的对话前端给个提示，但是前端也要给必要的排队数量限制，不超过3个
